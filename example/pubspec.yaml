name: background_runtime_example
description: "Example app for the background_runtime plugin."
publish_to: 'none'
version: 1.0.0+1

resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  background_runtime: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
