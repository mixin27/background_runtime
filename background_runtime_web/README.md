# background_runtime_web

Web implementation of the [background_runtime](https://github.com/mixin27/background_runtime) Flutter plugin.

Best-effort support. Some native features (foreground service, audio playback) are unavailable on the web platform.
